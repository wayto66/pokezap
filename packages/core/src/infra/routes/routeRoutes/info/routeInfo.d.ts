import { TRouteParams } from '../../../../infra/routes/router';
import { IResponse } from '../../../../server/models/IResponse';
export declare const routeInfo: (data: TRouteParams) => Promise<IResponse>;
